# NetReaper GUI - Windows Edition Architecture

Visual architecture and component overview.

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     NetReaper GUI Windows                        │
│                         (PyQt6 Application)                      │
└─────────────────────────────────────────────────────────────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
         ┌──────────▼──────────┐   ┌─────────▼──────────┐
         │   GUI Layer         │   │  Execution Layer   │
         │   (PyQt6 Widgets)   │   │  (CommandThread)   │
         └──────────┬──────────┘   └─────────┬──────────┘
                    │                         │
         ┌──────────▼──────────┐   ┌─────────▼──────────┐
         │  Business Logic     │   │  Platform Layer    │
         │  (Tab Controllers)  │   │  (Windows/Linux)   │
         └──────────┬──────────┘   └─────────┬──────────┘
                    │                         │
         ┌──────────▼──────────┐   ┌─────────▼──────────┐
         │  Data Layer         │   │  System Layer      │
         │  (Config/History)   │   │  (PowerShell/Bash) │
         └─────────────────────┘   └────────────────────┘
```

## 📦 Component Hierarchy

```
NetReaperGui (Main Window)
├── QToolBar (Actions)
│   ├── Clear Log Action
│   ├── Stop Tasks Action
│   ├── Lite Mode Toggle
│   └── Edit Config Action
│
├── ReaperHeader (Session Info)
│   ├── Title & Subtitle
│   ├── Stats Grid
│   │   ├── Session ID
│   │   ├── Time
│   │   ├── Ingress Status
│   │   └── Target Info
│   └── Navigation Buttons
│
├── HUDPanel (Animated Status)
│   ├── Status Label
│   ├── Bio-Pulse Counter
│   └── Latency Display
│
├── QTabWidget (Main Content)
│   ├── ScanTab
│   │   ├── TargetField
│   │   ├── Preset Selector
│   │   └── Scan Buttons
│   │
│   ├── ReconTab
│   │   ├── TargetField
│   │   ├── Discovery Group
│   │   └── Enumeration Group
│   │
│   ├── WebTab
│   │   ├── TargetField
│   │   ├── Scanner Group
│   │   └── Directory Group
│   │
│   ├── WirelessTab
│   │   ├── Interface Field
│   │   ├── WiFi Tools (Windows)
│   │   └── Advanced Tools (Linux)
│   │
│   └── ToolsTab (Windows Only)
│       ├── System Tools Group
│       └── Network Tools Group
│
├── Terminal Area
│   ├── QPlainTextEdit (Output Log)
│   └── QLineEdit (Command Input)
│
└── QListWidget (Command History)
```

## 🔄 Data Flow

### Command Execution Flow

```
User Input
    │
    ▼
┌─────────────────┐
│  GUI Component  │ (Button Click / Enter Key)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Executor       │ (execute_command method)
│  - Validate     │
│  - Sanitize     │
│  - Build Cmd    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ CommandThread   │ (Async Execution)
│  - Create       │
│  - Start        │
│  - Monitor      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Platform Layer  │ (Windows/Linux)
│  - PowerShell   │
│  - Bash         │
│  - CMD          │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ System Commands │ (Native Tools)
│  - nmap         │
│  - netsh        │
│  - ipconfig     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Output Stream   │ (Real-time)
│  - stdout       │
│  - stderr       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ GUI Update      │ (Signal/Slot)
│  - Append Text  │
│  - Update Stats │
│  - Show Status  │
└─────────────────┘
```

### Configuration Flow

```
Application Start
    │
    ▼
┌─────────────────┐
│ Load Config     │
│  - Read File    │
│  - Parse        │
│  - Validate     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Apply Settings  │
│  - Lite Mode    │
│  - Wordlist     │
│  - Defaults     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ User Changes    │
│  - Edit Config  │
│  - Toggle Mode  │
│  - Set Options  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Save Config     │
│  - Validate     │
│  - Write File   │
│  - Confirm      │
└─────────────────┘
```

## 🧩 Class Relationships

```
QWidget
    │
    ├── NetReaperGui (Main Window)
    │   ├── Uses: ReaperHeader
    │   ├── Uses: HUDPanel
    │   ├── Uses: ScanTab
    │   ├── Uses: ReconTab
    │   ├── Uses: WebTab
    │   ├── Uses: WirelessTab
    │   └── Uses: ToolsTab
    │
    ├── CategoryTab (Base Class)
    │   ├── Inherited by: ScanTab
    │   ├── Inherited by: ReconTab
    │   ├── Inherited by: WebTab
    │   ├── Inherited by: WirelessTab
    │   └── Inherited by: ToolsTab
    │
    ├── TargetField (Reusable Component)
    │   └── Used by: All Tabs
    │
    ├── ReaperHeader (Session Display)
    │   └── Used by: NetReaperGui
    │
    └── HUDPanel (Status Display)
        └── Used by: NetReaperGui

QThread
    │
    └── CommandThread (Async Execution)
        └── Used by: NetReaperGui
```

## 🔌 Platform Abstraction

```
┌─────────────────────────────────────────────────────────┐
│              Application Layer (Python)                  │
│  - GUI Components                                        │
│  - Business Logic                                        │
│  - Data Management                                       │
└────────────────────┬────────────────────────────────────┘
                     │
         ┌───────────┴───────────┐
         │                       │
┌────────▼────────┐    ┌────────▼────────┐
│  Windows Layer  │    │   Linux Layer   │
│  - PowerShell   │    │   - Bash        │
│  - CMD          │    │   - sh          │
│  - netsh        │    │   - aircrack-ng │
│  - ipconfig     │    │   - iwconfig    │
└────────┬────────┘    └────────┬────────┘
         │                       │
         └───────────┬───────────┘
                     │
         ┌───────────▼───────────┐
         │   Common Interface    │
         │  - quote()            │
         │  - get_shell()        │
         │  - get_config_dir()   │
         │  - get_temp_dir()     │
         └───────────────────────┘
```

## 🎨 UI Component Structure

```
Main Window (1280x900)
┌─────────────────────────────────────────────────────────┐
│ ┌─────────────────────────────────────────────────────┐ │
│ │              Toolbar (Actions)                       │ │
│ └─────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────┐ │
│ │         ReaperHeader (Session Info)                  │ │
│ │  Session: ABC123  |  Time: 12:34:56                 │ │
│ │  Ingress: ONLINE  |  Target: 192.168.1.1            │ │
│ └─────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────┐ │
│ │           HUDPanel (Animated Status)                 │ │
│ │  Status: STANDBY | Pulse: 45% | Latency: 12ms       │ │
│ └─────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────┐ │
│ │  ┌──┬──┬──┬──┬──┐                                   │ │
│ │  │SC│RE│WE│WI│TO│  Tab Widget                       │ │
│ │  └──┴──┴──┴──┴──┘                                   │ │
│ │  ┌─────────────────────────────────────────────┐   │ │
│ │  │                                               │   │ │
│ │  │         Tab Content Area                      │   │ │
│ │  │         (Scrollable)                          │   │ │
│ │  │                                               │   │ │
│ │  │  - Target Field                               │   │ │
│ │  │  - Action Buttons                             │   │ │
│ │  │  - Options                                    │   │ │
│ │  │                                               │   │ │
│ │  └─────────────────────────────────────────────┘   │ │
│ └─────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────┐ │
│ │  Output Log (120px height)                          │ │
│ │  [command output appears here...]                   │ │
│ └─────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────┐ │
│ │  Command Input: [type command here]                 │ │
│ └─────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────┐ │
│ │  Command History                                     │ │
│ │  - Previous Command 1                                │ │
│ │  - Previous Command 2                                │ │
│ └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

## 🔐 Security Architecture

```
User Input
    │
    ▼
┌─────────────────┐
│  Input Layer    │
│  - Validation   │
│  - Sanitization │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Security Layer  │
│  - Quote Paths  │
│  - Escape Chars │
│  - Check Tools  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Execution Layer │
│  - Subprocess   │
│  - Isolation    │
│  - Monitoring   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Output Layer   │
│  - Filter       │
│  - Log          │
│  - Display      │
└─────────────────┘
```

## 📊 State Management

```
Application State
├── Session State
│   ├── session_id: str
│   ├── ingress_online: bool
│   ├── active_threads: List[CommandThread]
│   └── lite_mode: bool
│
├── UI State
│   ├── current_tab: int
│   ├── target_fields: List[TargetField]
│   └── output_log: QPlainTextEdit
│
├── Data State
│   ├── target_history: List[str]
│   ├── command_history: List[str]
│   └── custom_wordlist: str
│
└── Config State
    ├── config_dir: str
    ├── temp_dir: str
    └── settings: Dict[str, Any]
```

## 🔄 Event Flow

```
User Action
    │
    ▼
┌─────────────────┐
│  Qt Event       │
│  - Click        │
│  - KeyPress     │
│  - Timer        │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Signal         │
│  - clicked()    │
│  - returnPressed│
│  - timeout()    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Slot           │
│  - Handler      │
│  - Method       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Business Logic │
│  - Process      │
│  - Execute      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  UI Update      │
│  - Refresh      │
│  - Display      │
└─────────────────┘
```

## 🧪 Testing Architecture

```
Testing Layers
├── Unit Tests (Planned)
│   ├── Component Tests
│   ├── Function Tests
│   └── Class Tests
│
├── Integration Tests (Planned)
│   ├── Tab Integration
│   ├── Command Execution
│   └── Platform Integration
│
├── Manual Tests (Current)
│   ├── TESTING_CHECKLIST.md
│   ├── Feature Tests
│   └── UI Tests
│
└── System Tests (Planned)
    ├── End-to-End Tests
    ├── Performance Tests
    └── Security Tests
```

## 📁 File Organization

```
Net.Reaper-rebuild-main/
├── Application
│   ├── netreaper_gui_windows.py    (Main app)
│   └── requirements_windows.txt     (Dependencies)
│
├── Launchers
│   ├── launch_gui.bat               (Batch launcher)
│   ├── launch_gui.ps1               (PowerShell launcher)
│   └── create_shortcut.ps1          (Shortcut creator)
│
├── Documentation
│   ├── README_WINDOWS.md            (User guide)
│   ├── INSTALL_WINDOWS.md           (Installation)
│   ├── QUICKSTART_WINDOWS.md        (Quick start)
│   ├── WINDOWS_FEATURES.md          (Features)
│   ├── WINDOWS_RELEASE_NOTES.md     (Release notes)
│   ├── TESTING_CHECKLIST.md         (Testing)
│   ├── WINDOWS_PACKAGE_SUMMARY.md   (Package info)
│   └── ARCHITECTURE.md              (This file)
│
└── Runtime (Created at runtime)
    ├── %APPDATA%\NetReaper\
    │   ├── config.conf              (Configuration)
    │   └── history\
    │       └── targets.log          (Target history)
    └── %TEMP%\
        └── netreaper_*.log          (Temporary logs)
```

## 🚀 Deployment Architecture

```
Development
    │
    ▼
┌─────────────────┐
│  Source Code    │
│  - Python       │
│  - Markdown     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Package        │
│  - Zip/Tar      │
│  - Git Clone    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Installation   │
│  - pip install  │
│  - Dependencies │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Configuration  │
│  - First Run    │
│  - Setup        │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Runtime        │
│  - Execute      │
│  - Monitor      │
└─────────────────┘
```

## 🔧 Extension Points

```
Extensibility
├── Custom Tools
│   └── Add new tool integrations
│
├── Custom Tabs
│   └── Create new tab categories
│
├── Custom Commands
│   └── Add command templates
│
├── Custom Themes
│   └── Modify BIO_LAB_THEME
│
└── Custom Plugins (Planned)
    └── Plugin system architecture
```

---

**NetReaper Windows Edition Architecture**  
Version 1.0.0  
© 2025 Nerds489
